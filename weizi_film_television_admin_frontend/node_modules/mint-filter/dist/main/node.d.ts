export default class Node {
    depth: number;
    key: string;
    word: boolean;
    children: Record<string, Node>;
    fail?: Node;
    count: number;
    constructor(key: string, depth?: number);
}
