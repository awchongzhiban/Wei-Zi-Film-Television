import { defineComponent } from 'vue';
import { CLASSES } from '../constants';
var ContextmenuIcon = defineComponent({
    name: 'VContextmenuIcon',
    props: {
        name: {
            type: String,
            required: true,
        },
    },
    render: function () {
        return (<i class={[
                CLASSES.contextmenuIcon,
                "".concat(CLASSES.contextmenuIcon, "-").concat(this.name),
            ]}/>);
    },
});
export default ContextmenuIcon;
//# sourceMappingURL=ContextmenuIcon.jsx.map